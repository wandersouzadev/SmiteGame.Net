namespace SmiteGame.Net.Models.Enums
{
    /// <summary>
    ///  A “Portal” is a gateway into our games via an identifier.
    /// </summary>
    public enum PortalId
    {
        Hirez = 1,
        Steam = 5,
        Psn = 9,
        XboxLive = 10,
        Switch = 22,
        Discord = 25,
        Epic = 28
    }
}
